import { ShowDetailDirective } from './show-detail.directive';

describe('ShowDetailDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowDetailDirective();
    expect(directive).toBeTruthy();
  });
});
